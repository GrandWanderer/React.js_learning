# Travel Destinations

Static React/Vite application for Laboratory Work No. 2 on the topic "Components and Data Passing".

The project demonstrates:
- component hierarchy in React
- one-way data flow through props
- rendering a destination list with `map()`
- presentation components without `useState`

Run locally:

```bash
npm install
npm run dev
```
